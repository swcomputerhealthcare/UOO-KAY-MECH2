"use client";

/**
 * SmoothScroll component wrapper.
 * Returns children directly for stable native browser scrolling.
 */
export default function SmoothScroll({ children }) {
  return <>{children}</>;
}
