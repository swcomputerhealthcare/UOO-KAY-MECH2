"use client";

/**
 * PageTransition wrapper component.
 * Immediately returns children for instant, stable page changes.
 */
export default function PageTransition({ children }) {
  return <>{children}</>;
}


